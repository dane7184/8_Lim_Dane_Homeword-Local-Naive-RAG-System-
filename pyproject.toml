[tool.poetry]
name = "rag-homework"
version = "0.1.0"
description = "Week 5 homework: baseline Naive RAG chat-with-documents app"
authors = ["Student <student@example.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<3.15"
chromadb = ">=0.5.0"
ollama = "^0.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
